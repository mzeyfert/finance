name: build

on: push

jobs:
  unit:
    steps:
      - run: echo "Run unit tests"
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -la
      - run: make build
      - run: make show
    needs: unit
